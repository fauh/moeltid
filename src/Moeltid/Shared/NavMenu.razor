<nav class="app-topnav" aria-label="Main navigation">
    <a class="navbar-brand" href="/">Måltid</a>

    <div class="nav-actions">
        <NavLink class="nav-link" href="/" Match="NavLinkMatch.All">Home</NavLink>
        <NavLink class="nav-link" href="/new">New event</NavLink>
        <button class="btn-theme-toggle" @onclick="ToggleTheme" aria-label="Toggle dark mode" title="Toggle dark mode">
            @(_isDark ? "☀️" : "🌙")
        </button>
    </div>
</nav>

@code {
    private bool _isDark;

    private async Task ToggleTheme()
    {
        _isDark = !_isDark;
        await JS.InvokeVoidAsync("document.documentElement.setAttribute", "data-theme", _isDark ? "dark" : "light");
    }

    [Inject] private IJSRuntime JS { get; set; } = default!;
}

